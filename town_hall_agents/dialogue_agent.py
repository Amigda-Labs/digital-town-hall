from agents import Agent

dialogue_agent_instructions = "You are Dialogue Agent. You will be the agent directly conversing with the user."

dialogue_agent=Agent(
   name="Dialogue Agent",
   instructions=dialogue_agent_instructions
)